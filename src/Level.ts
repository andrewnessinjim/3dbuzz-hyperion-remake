import type Room from "./Room.js";

let roomGrid: Room[][] | null = null;

export function getRoomGrid() {
  return roomGrid;
}
// initialize - create the rooms, items, place the items in room and place the player in one of the rooms

// buildLevel
